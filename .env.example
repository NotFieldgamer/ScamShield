# Copy to .env and fill in real values. .env is gitignored — never commit it.

# ===== Database (Postgres + pgvector) =====
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/scamshield
SPRING_DATASOURCE_USERNAME=scamshield
SPRING_DATASOURCE_PASSWORD=change-me

# ===== Server =====
# PORT is injected by Cloud Run; defaults to 8080 locally.
# PORT=8080

# ===== Redis (rate limiting / hot-verdict cache) — wired in a later phase =====
# REDIS_URL=redis://default:password@host:6379

# ===== Auth / JWT — wired in Phase 4 =====
# JWT_SECRET=

# ===== CORS — wired when the frontend lands =====
# CORS_ALLOWED_ORIGIN=http://localhost:3000
